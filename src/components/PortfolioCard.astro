---
import AnimatedImage from '@/components/AnimatedImage.astro';
import type { NotionRecord } from '@/types/NotionRecord';

interface Props {
  page: NotionRecord;
  basePath: string;
  dataTags?: string;
}

const { page, basePath, dataTags } = Astro.props;
---

<article class="work-item" data-tags={dataTags}>
  <a href={`${basePath}/${page.slug}`} class="card card-item">
    <AnimatedImage src={page.image ?? ''} alt={page.title ?? ''} class="aspect-video" />
    <div class="card-body p-0 pt-2">
      <h2 class="card-title fade-in-text opacity-0 translate-y-8">{page.title}</h2>
      <p class="fade-in-text opacity-0 translate-y-8">{page.summary}</p>
    </div>
  </a>
</article>
